/**
 * Alert select – layout for the alerts picker screen (root-level route).
 * Screen options (modal/formSheet) are set on the root Stack in _layout.tsx.
 */

import React from 'react';
import { Slot } from 'expo-router';

export default function AlertSelectLayout() {
  return <Slot />;
}
