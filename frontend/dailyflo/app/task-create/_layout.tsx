/**
 * Task create – layout for the full-screen create-task form.
 * Presentation (fullScreenModal, drag to close) is set on the root Stack in _layout.tsx.
 */

import React from 'react';
import { Slot } from 'expo-router';

export default function TaskCreateLayout() {
  return <Slot />;
}
