/**
 * Completions select – layout for the habit completions picker (root-level route).
 * Screen options (modal/formSheet) are set on the root Stack in _layout.tsx.
 */

import React from 'react';
import { Slot } from 'expo-router';

export default function HabitCompletionsSelectLayout() {
  return <Slot />;
}
