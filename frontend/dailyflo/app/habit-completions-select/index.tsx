/**
 * Route: /habit-completions-select
 * Root stack picker — stacks on habit detail formSheet (same tier as /time-duration-select).
 */

import { HabitCompletionsSelectScreen } from '@/components/features/habits/detail/modals';

export default function HabitCompletionsSelectRoute() {
  return <HabitCompletionsSelectScreen />;
}
