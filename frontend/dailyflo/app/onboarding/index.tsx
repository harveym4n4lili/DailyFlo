/**
 * standalone questionnaire route — same flow as `(onboarding)/slides`.
 */

import { OnboardingQuestionnaireFlow } from '@/components/features/onboarding';

export default function OnboardingQuestionnaireScreen() {
  return <OnboardingQuestionnaireFlow />;
}
