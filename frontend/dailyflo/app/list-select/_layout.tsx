/**
 * list-select — root route layout; stack options (formSheet / liquid glass) live in app/_layout.tsx.
 */

import React from 'react';
import { Slot } from 'expo-router';

export default function ListSelectLayout() {
  return <Slot />;
}
