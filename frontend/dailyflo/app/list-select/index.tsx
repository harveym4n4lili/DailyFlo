/**
 * Route: /list-select — list destination sheet (same stack treatment as /date-select).
 */

import { ListSelectScreen } from '@/components/features/tasks/TaskScreen/modals';

export default function ListSelectRoute() {
  return <ListSelectScreen />;
}
