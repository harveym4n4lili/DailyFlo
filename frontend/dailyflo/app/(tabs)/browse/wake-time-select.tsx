/**
 * Route: (tabs)/browse/wake-time-select
 * liquid glass sheet for wake time — opened from settings calendar section.
 */

import { ScheduleTimeSelectScreen } from '@/components/features/settings/screens/ScheduleTimeSelectScreen';

export default function BrowseWakeTimeSelectRoute() {
  return <ScheduleTimeSelectScreen />;
}
