import { InboxTaskListContent } from '@/components/features/inbox/InboxTaskListContent';

/** browse stack push — back button returns to browse home */
export default function BrowseInboxScreen() {
  return <InboxTaskListContent chromeVariant="browse-stack" />;
}
