/**
 * Route: (tabs)/browse/sleep-time-select
 * liquid glass sheet for sleep time — opened from settings calendar section.
 */

import { ScheduleTimeSelectScreen } from '@/components/features/settings/screens/ScheduleTimeSelectScreen';

export default function BrowseSleepTimeSelectRoute() {
  return <ScheduleTimeSelectScreen />;
}
