/**
 * Activity log – layout for the full-screen activity log stack screen.
 * Same structure as task-create: presentation (formSheet, drag to close) is set on the root Stack in _layout.tsx.
 */

import React from 'react';
import { Slot } from 'expo-router';

export default function ActivityLogLayout() {
  return <Slot />;
}
