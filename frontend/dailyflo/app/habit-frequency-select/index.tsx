/**
 * Route: /habit-frequency-select
 * Root stack picker — stacks on habit detail formSheet (same tier as /date-select).
 */

import { HabitFrequencySelectScreen } from '@/components/features/habits/detail/modals';

export default function HabitFrequencySelectRoute() {
  return <HabitFrequencySelectScreen />;
}
