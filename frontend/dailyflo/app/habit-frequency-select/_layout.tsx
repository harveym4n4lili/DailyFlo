/**
 * Frequency select – layout for the habit frequency picker (root-level route).
 * Screen options (modal/formSheet) are set on the root Stack in _layout.tsx.
 */

import React from 'react';
import { Slot } from 'expo-router';

export default function HabitFrequencySelectLayout() {
  return <Slot />;
}
