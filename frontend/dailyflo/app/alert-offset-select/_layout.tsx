/**
 * Alert offset select – layout for the add-alert spinner screen (root-level route).
 */

import React from 'react';
import { Slot } from 'expo-router';

export default function AlertOffsetSelectLayout() {
  return <Slot />;
}
