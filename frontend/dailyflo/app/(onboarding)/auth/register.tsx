/**
 * onboarding email register sheet route — mounts `AuthRegisterScreen`.
 */

import { AuthRegisterScreen } from '@/components/features/onboarding/auth/screens/AuthRegisterScreen';

export default function OnboardingAuthRegisterRoute() {
  return <AuthRegisterScreen />;
}
