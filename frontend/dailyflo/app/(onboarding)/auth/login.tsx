/**
 * onboarding email login sheet route — mounts `AuthLoginScreen` from feature folder.
 */

import { AuthLoginScreen } from '@/components/features/onboarding/auth/screens/AuthLoginScreen';

export default function OnboardingAuthLoginRoute() {
  return <AuthLoginScreen />;
}
