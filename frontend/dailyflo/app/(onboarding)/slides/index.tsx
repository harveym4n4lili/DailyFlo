/**
 * post-intro questionnaire — steps driven by text continue CTA (see `OnboardingQuestionnaireFlow`).
 */

import { OnboardingQuestionnaireFlow } from '@/components/features/onboarding';

export default function OnboardingSlidesScreen() {
  return <OnboardingQuestionnaireFlow />;
}
