/**
 * Route: /habit-reminder-select
 * Root stack picker — stacks on habit detail formSheet (same tier as /time-duration-select).
 */

import { HabitReminderSelectScreen } from '@/components/features/habits/detail/modals';

export default function HabitReminderSelectRoute() {
  return <HabitReminderSelectScreen />;
}
