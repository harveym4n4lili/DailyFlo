/** shared timing for ai empty-state intro — greeting text + background fade together */
export const AI_EMPTY_STATE_GREETING_FADE_MS = 480;

/** greeting + hint fade out together when the first message is sent */
export const AI_EMPTY_STATE_GREETING_FADE_OUT_MS = 480;

/** delay between each hint character in the typewriter animation */
export const AI_EMPTY_STATE_HINT_CHAR_DELAY_MS = 36;
