/**
 * intro route uses a horizontal pager — dot count must match ScrollView page children.
 * change here when beats are added/removed so header + pager stay aligned.
 */
export const ONBOARDING_INTRO_PAGE_COUNT = 2;
