/**
 * auth landing — visible headline for the single pre-questionnaire screen.
 */

import type { AuthPageTitleConfig } from './types';

export const AUTH_PAGE_TITLE: AuthPageTitleConfig = {
  title: 'DailyFlo',
  titleStyle: {
    fontSize: 54,
    fontWeight: '400',
  },
};
