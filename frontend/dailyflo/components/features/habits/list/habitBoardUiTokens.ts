/**
 * habit board card layout tokens — tweak ring + center plus icon on habit screen cards.
 */

/** diameter (px) of the increment ring on HabitBoardCard */
export const HABIT_BOARD_RING_SIZE = 36;

/** ring arc stroke width (px) — track + progress circles */
export const HABIT_BOARD_RING_STROKE_WIDTH = 3.75;

/** center plus icon size (px) */
export const HABIT_BOARD_PLUS_ICON_SIZE = 16;

/** center plus icon stroke width (px) */
export const HABIT_BOARD_PLUS_STROKE_WIDTH = 3.75;
